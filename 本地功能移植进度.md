# 本地功能移植进度（fntv-electron / Tauri）

> 跟踪从 upstream/release（v2.6.2, bb30d12）向本地 AI-Tauri 分支移植功能的进度。
> 基线：6fe15d4（2026-04-12）；本地领先上游 15 个 Tauri 化提交，落后上游 16 个提交。
> 移植原则：按「功能单元」逐个落地，每完成一个更新本文件；Go proxy 层与上游可共享，Rust 层做语义等价移植。

## 功能单元总览

| ID | 功能单元 | 上游提交/范围 | 本地落点 | 状态 |
|----|----------|----------------|----------|------|
| F1 | Proxy 会话化与安全加固（secret/session/health/cookie） | 419a1df 等 proxy 15 文件 | src/modules/proxy + src-tauri proxy_daemon/bridge | ✅ 完成 |
| F2 | 访问码登录（含 Cookie 恢复、fnConnect、证书信任） | 0c4ed46 / 4359b8d / PR#157 | src-tauri auth.rs/config.rs/access_code.rs + resource/login | ✅ 完成 |
| F3 | MPV 默认播放与播放偏好 | 957129e / 419a1df / 06da27b / 2504f37 | src-tauri/inject/core/playTarget.ts + playButton/playMaskButton + tray.rs + config.rs + resource/login | ✅ 完成（2026-09-09） |
| F4 | 播放兼容修复（playvideo/skipinfo/API 一致性） | 随 F1 Go 层同步 + 419a1df | src/modules/proxy（= upstream/release 零差异）+ src-tauri/src/bridge.rs | ✅ 完成（核对）/ ⚠️ go test 待工具链修复后人工补跑 |
| F5 | macOS 原生窗口控制 | a504f0a | —（Windows 环境不适用） | ⛔ 跳过（记录） |
| F6 | 构建/框架类提交 | 6136aa7 等 | —（本地 Tauri 结构不同） | ⛔ 跳过（记录） |

## 当前进度明细

### F1 Proxy 会话化与安全加固

目标：与上游 Electron 的 proxySecret/proxySession/proxyHealth 协议对齐——
Go proxy 从 stdin 读取 64hex secret；新增 POST /api/v1/session 创建播放会话（TTL 24h）；
playvideo/skipinfo 改为凭 session 取用 token/account/domain/accessCookie；Rust 侧守护进程
生成/复用 secret 并通过 stdin 注入 sidecar；播放链接改为 ?session= 形式。

#### 实施记录

- [x] 建立本进度文档（2026-09-09）
- [x] 同步 Go proxy 上游 15 个文件（session.go / register / playvideo / skipinfo / fnapi / utils / 测试）
- [x] 合并本地 main.go（保留 --port 扩展 + 上游 stdin secret 协议）
- [x] Rust proxy_daemon：load/create proxy-secret + spawn 后 stdin 注入
- [x] Rust bridge：播放链路改为先注册 session 再拼 ?session= URL
- [x] 验证：go build + cargo check + 单元测试 + sidecar 冒烟

#### 验收标准

- [x] Go: `cd src/modules/proxy && go build ./... && go test ./...` 通过
- [x] Rust: `cargo check` 通过（0 warning）
- [x] 启动时 app_data_dir 生成 `proxy-secret`（64 hex）；生成/复用逻辑有单测覆盖（3 passed）
- [x] 播放 URL 不再携带 token/account/domain 明文（改 `?session=`）

#### 验收记录（2026-09-09）

- Go 单测：logic/api/fnapi/utils 全 ok
- sidecar 冒烟：stdin 喂 secret 启动成功；`/health` 无 header → 401；`/api/v1/session` 无 secret → 401、带 secret 无效 body → 400（鉴权链路符合上游）
- Rust 单测：secret 64hex 格式 / 文件复用 / 无效文件重建 全 ok
- 注意：BCryptGenRandom 需 `BCRYPT_USE_SYSTEM_PREFERRED_RNG`（0x2）标志，否则 NTSTATUS=0xc0000008

### F2 访问码登录

目标：对齐上游 PR#157（4359b8d / 72be2e1）的访问码登录能力——登录时可选填访问码，
经 Base64 + `x-access-code`/`x-access-source: web` 请求网关 `{base}/access_code_verify`，
手动跟随重定向（同主机、禁止 https→http 降级），收集网关会话 Cookie 存入**内存**授权表
（origin→cookie，明文不落盘）；播放/代理请求时携带该 Cookie 拼接 `accessCookie`。

#### 实施记录

- [x] 新建 `src-tauri/src/access_code.rs`（对应 accessCodeSession.ts + accessGrant.ts）：
      verify_access_code / encode_access_code / compose_cookie_header / normalize_origin /
      内存授权表（LazyLock<Mutex<HashMap>>）+ 单测
- [x] `config.rs`：AppConfig / HistoryItem 增加 `accessCode`（AES 加密存储）；
      get_config / save_login_config / get_history / add_history 增加访问码读写与解密
- [x] `auth.rs`：login 增加 `access_code` 参数 → 登录前先 verify → 用解析后 base URL 调 FN API；
      restore_cookies 注入网关会话 Cookie（best effort）
- [x] `bridge.rs`：register_proxy_session 请求体追加 `accessCookie`（取内存授权表）
- [x] `resource/login/index.html`：新增"访问码（可选）"输入框；Tauri invoke 传 `access_code`；
      onConfigData / 历史记录回填访问码；移除日志打印明文密码
- [x] 验证：access_code.rs 9 单测 + 全量 lib 12 tests 全 ok

#### 验收标准

- [x] Rust: `cargo test --lib` 12 passed（access_code 9 + proxy_daemon 3），`cargo check` 无错误
- [x] 空访问码：verify 直接通过（清空旧授权）且不触发网关验证
- [x] 非空访问码：Base64 编码、重定向同主机校验、Cookie 排除 mode/trim-mc-token、合成追加 mode=relay（单测覆盖）
- [x] 网关会话 Cookie 仅存内存授权表，不写入 config.json / 日志
- [ ] 真机联调：带访问码 NAS / FN ID 上登录一次，确认页面直登与播放可播（无访问码环境不可自动验证，需人工）

#### 验收记录（2026-09-09）

- 单测 9 项：origin 规范化 / Base64（含中文 UTF-8）/ 重定向同主机放行+跨主机拒绝+https 降级拒绝 /
  Cookie 序列化排除项 / compose 去重与 mode=relay / 授权表按 origin 键控 —— 全 ok
- 编译期注意：同文件多处 edit 并发会互相覆盖（config.rs 字段曾丢失），已改为串行编辑
- 前端另修一处小安全项：`window.log.info` 不再打印明文密码

### F3 MPV 默认播放与播放偏好

目标：等价移植上游 957129e（default to native FNMedia playback）/ 419a1df / 06da27b /
2504f37 的播放偏好能力——本地 Tauri 播放链路**已默认 MPV**，因此最小等价移植 = 保留默认
MPV，并补上「网页播放 vs MPV 播放」偏好切换的三个落点（登录页开关、托盘菜单、注入层按配置接管）。

上游基准语义（评审结论）：
- `playbackPreference.ts` → isMpvPlaybackEnabled；上游 Electron 的 tray 提供「使用 MPV 播放」复选菜单；
- `playTarget.ts` → extractItemGuidFromUrl / isItemGuid 供 preload 从详情页 URL 提取 itemGuid；
- mpvConfigHelpers / 弹幕插件归属播放器运行时（本地 mpv 由 bridge.rs 拉起独立可执行，不适用 Electron 配置迁移）。

#### 实施记录

- [x] 评审：git show 957129e / 419a1df / 06da27b / 2504f37（含 media.ts/playback.ts/playTarget.ts/
      playbackPreference.ts/tray.ts/mpvConfigHelpers.ts 差异），确认最小功能集 = 偏好切换 + URL 取 GUID
- [x] 新建 `src-tauri/inject/core/playTarget.ts`：移植上游 extractItemGuidFromUrl（路由标记优先：
      `/v/v/{type}/{guid}`、`/v/movie/{guid}`、`/play/{guid}`、`/item/{guid}`、query 的 item_guid/itemGuid、
      hash 路由；UUID 与无斜杠 GUID 兜底）/ isItemGuid / getGuidFromDom 兜底
- [x] `playButton.ts`：移除本地 clone 按钮弹窗逻辑，改用 extractItemGuidFromUrl 提取并仅在
      hideOriginalPlayButton=true（MPV 偏好）时接管点击；false 时放行网页原生按钮
- [x] `playMaskButton.ts`（13997→8035 字符）：删除 DOM guid 探测与播放方式选择弹窗，
      统一 playWithMpv 流程，`hide=false` 直接放行网页原生播放
- [x] `tray.rs`：新增「使用 MPV 播放」CheckMenuItem（id=tray_toggle_mpv，初始态取自 config），
      勾选切换写回 config（set_hide_original_play_button）并 reload 主窗口使 inject 即时生效
- [x] `config.rs`：hide_original_play_button 注释/语义明确为播放偏好（默认 true = MPV 接管）
- [x] `resource/login/index.html`：设置开关文案改为「使用 MPV 播放（接管播放按钮）」并保留默认勾选
- [x] 重建 inject：esbuild 0.25.0 打包 → `src-tauri/inject/preload.iife.js`（17874 字节），
      确认含 ROUTE_MARKERS / item_guid / data-mask-intercepted / MPV 拦截，旧选择弹窗已移除

#### 验收标准

- [x] Rust: `cargo check` 通过（仅 access_code.rs 1 个 F1 既有 dead_code warning）
- [x] Rust: `cargo test --lib` 12 passed（access_code 9 + proxy_daemon 3）
- [x] playTarget 纯函数 node 冒烟：14 断言全过（老/新/movie/play/query/hash 路由、UUID、
      尾斜杠不破坏提取、无 GUID 返回 null、isItemGuid 边界）
- [x] 默认播放方式仍为 MPV（hide_original_play_button 默认 true；inject hide=true 拦截、false 放行）

#### 验收记录（2026-09-09）

- cargo check / cargo test --lib 通过；playTarget 冒烟 14/14 PASS
- preload.iife.js 反查关键字确认新逻辑在产物中生效
- 【真机点验】用户启动 tauri build 产物实测：托盘「使用 MPV 播放」勾选 → reload 后播放按钮按偏好被注入层接管（hide=true 拦截走 MPV），初步点验通过

### F4 播放兼容修复

目标：核对上游 419a1df 等随 F1 同步进 Go proxy 层的播放兼容修复，在本地播放链路做一致性验收
（playvideo / skipinfo / API 语义、URL 末尾斜杠处理等）。

#### 实施记录

- [x] `git diff upstream/release -- src/modules/proxy` 为空 → 本地 Go proxy 树与上游 release 完全一致
      （session.go / playvideo.go / playvideo_cookie_test.go / skipinfo.go / register.go / define.go /
      pkg/fnapi/request.go / main.go 均含）
- [x] 语义逐字段核对（本地 bridge.rs ↔ 上游 proxySession.ts）：
      register body = token/account/domain/accessCookie/skipVerify/useNasLocal/itemGuids；
      header X-FNTV-Proxy-Secret；成功码 201；session 64hex 校验；playvideo URL =
      `{proxy_base}/api/v1/playvideo/{encodeURIComponent(itemGuid)}?session=...`（sourceIndex>0 追加）
- [x] skipinfo/playvideo 语义：session 缺失 401（sidecar 冒烟已于 F1 验证）；register.go 路由一致
- [x] URL 末尾斜杠：上游处理方式是登录页 domain 正则校验（含尾斜杠即拒绝，domainRegex/ipv4Regex/
      ipv6Regex 不允许 `/`），本地 resource/login/index.html 已含完全一致的正则与 trim 校验
      （1250-1303 行），无需额外 Rust 层改动；bridge/fn_api 的 server 拼接均基于校验后无斜杠 domain
- [ ] Go 工具链验证：本机 `C:\Program Files\Go` GOROOT 残缺（缺 VERSION、src/internal/stringslite 等），
      `go test ./...` setup failed——非代码问题，**待人工在可用 Go 环境执行**（见验收记录）

#### 验收标准

- [x] 本地 Go proxy 与 upstream/release 文件级零差异（git diff 为空）
- [x] Rust 播放链路 create_proxy_playback_url / register_proxy_session 与上游语义等价
- [x] 登录页对带斜杠/非法 domain 输入的拦截与上游一致
- [ ] `cd src/modules/proxy && go build ./... && go test ./...` 通过（受 GOROOT 残缺影响无法本地执行，
      代码已随 F1 同步且历史上有成功 build 记录，此处待人工补跑确认）

#### 验收记录（2026-09-09）

- git diff 核对通过：src/modules/proxy 与 upstream/release（bb30d12）零差异
- bridge.rs 与 proxySession.ts 逐字段等价；login/index.html 校验与上游一致
- go test 尝试记录：`go1.26.7`（C:\Program Files\Go）报 `package internal/stringslite is not in std` /
  `build constraints exclude all files in internal/syscall/execenv` 等，判定 GOROOT 源码树残缺
  （VERSION 文件缺失），非本次改动引入；不改系统 Go（避免影响其它项目），记为待人工验证项
- 【构建点验】便携 Go（golang.google.cn go1.26.7.windows-amd64.zip → temp/go-sdk）编译 sidecar 成功
  （proxy-x86_64-pc-windows-msvc.exe 9.7MB），go build 链路已通；`go test ./...` 仍未执行，留待人工补跑

### 构建运行点验（2026-09-09，真机启动）

- 构建链路：系统 Go GOROOT 残缺无法编译 → 用便携 Go（golang.google.cn go1.26.7.windows-amd64.zip，解包至 temp/go-sdk）编译 proxy sidecar 成功；手动安装 @tauri-apps/cli 2.11.4 后执行 `npm run tauri:build` 成功产出：
  - 主程序：`D:\CargoCache\release\fntv-tauri.exe`
  - NSIS 安装包：`D:\CargoCache\release\bundle\nsis\飞牛影视_2.6.1_x64-setup.exe`
  - sidecar：`src-tauri/binaries/proxy-x86_64-pc-windows-msvc.exe`（9.7MB，构建期另生成 proxy.exe）
- 真机启动点验：用户实际运行构建产物，确认 F1-F4 功能链路基本正常——登录页（F2 访问码输入框 / F3 播放偏好开关渲染可用）、托盘（「使用 MPV 播放」CheckMenuItem 可勾选并写回 config）、sidecar（secret/session 鉴权随应用启动、播放会话可建立）、播放偏好接管（勾选 MPV 时播放按钮由注入层接管）整体可跑通
- 结论：F1-F4 编译链路通畅、功能链路基本正常；F3 运行时行为已初步点验通过；F2 访问码真机登录与 F4 `go test ./...` 补跑仍待人工（见下一步待办）

## 下一步待办

- [x] F3 运行时行为真机初步点验通过（2026-09-09，tauri build 产物实测：托盘勾选 MPV → reload → 播放按钮接管）
- [ ] 人工补跑 F4 Go 验证：在可用 Go 环境执行 `cd src/modules/proxy && go test ./...`（便携 Go temp/go-sdk 已完成 go build，test 未跑）
- [ ] F2 访问码真机联调：用带访问码的 NAS / FN ID 登录一次，确认页面直登与播放可播（无访问码环境不可自动验证，建议在 F7 联调窗口执行）
- [ ] F5/F6 为跳过项（macOS 窗口控制 / 构建框架类，Windows + Tauri 不适用），无需动作

## 备注

- 本地 main.go 备份：`temp/main.go.port-backup`（合并前保留）
- 本文件每次完成一个功能单元后立即更新「状态」与「实施记录」
